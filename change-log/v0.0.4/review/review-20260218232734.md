# PR Review Summary — myT-x コミット前レビュー

**レビュー日**: 2026-02-18
**対象**: `myT-x/` 配下の全変更ファイル (66ファイル, +7285/-2535行)
**レビュー方式**: カテゴリ別並列レビュー (6エージェント同時実行)

---

## レビューカテゴリ

| カテゴリ | ファイル数 | レビューアスペクト |
|---------|-----------|------------------|
| Backend App層 | 9 | code, errors, simplify |
| Backend tmux内部 | 15 | code, errors, types, concurrency |
| tmux-shim | 8 | code, errors, parsing |
| Frontend | 16 | code, errors, types, simplify |
| テスト品質 | 14 | tests, coverage |
| Config/Install/IPC | 5 | code, errors, security |

---

## Critical Issues（必須修正: 1件）

### C-1: `handleKillPane` でのターミナル二重 Close 🔴

**ファイル**: `myT-x/internal/tmux/command_router_handlers_pane.go` (L348-L380)
**カテゴリ**: Backend tmux内部

**問題**: `handleKillPane` で `target.Terminal` をキャプチャし、`KillPane()` 呼び出し後に `terminal.Close()` を実行している。しかし、`KillPane` → `killPaneLocked` の内部で **同じ Terminal を `closeTargets` に収集し、Lock 解放後に Close している**。

**コードフロー**:
```
handleKillPane:
  terminal := target.Terminal       // (1) キャプチャ
  KillPane(paneID)                  // (2) 内部で Terminal.Close() 実行
  terminal.Close()                  // (3) 同じ Terminal を再度 Close → 二重Close！
```

`killPaneLocked` (`session_manager_pane_lifecycle.go` L104-105):
```go
result.closeTargets = append(result.closeTargets, pane.Terminal)
pane.Terminal = nil
```

`KillPane` (`session_manager_pane_lifecycle.go` L216-222):
```go
for _, ct := range result.closeTargets {
    if closeErr := ct.Close(); closeErr != nil { ... }
}
```

**影響**: ConPTYハンドルの二重解放。パニックまたはリソースリークの原因。

**修正方法**: `handleKillPane` のターミナル Close コードを削除する。`KillPane` が内部でClose を完結させているため、呼び出し元での Close は不要。

```go
// 以下のコードを削除:
terminal := target.Terminal
// ...
if terminal != nil {
    if closeErr := terminal.Close(); closeErr != nil { ... }
}
```

---

## Important Issues（修正推奨: 15件）

### I-1: IME composition 終了後の保留リサイズがフラッシュされない 🟠

**ファイル**: `myT-x/frontend/src/hooks/useTerminalResize.ts` (L55-72)
**カテゴリ**: Frontend

**問題**: フック分離に伴い、旧コードの `finishComposition` → `scheduleResize` のフロー制御が切断されている。

1. IME composition 中に ResizeObserver が発火 → `scheduleResize` は `isComposingRef.current === true` で早期リターン、`pendingResize = true` を設定（タイマーは設定しない）
2. composition 終了 → `useTerminalEvents` の `finishComposition` が `isComposingRef.current = false` を設定
3. しかし `scheduleResize` を呼ぶトリガーがない → 保留リサイズは次の ResizeObserver コールバックまで反映されない

**修正方法**: `useTerminalResize` に composition 終了後のフラッシュ機構を追加、または `useTerminalEvents` の `finishComposition` からリサイズフラッシュを呼び出すコールバックを受け取る設計に変更。

---

### I-2: `resolveWindowPaneTarget` が nil pane を返しうる 🟠

**ファイル**: `myT-x/internal/tmux/session_manager_targets.go` (L122)
**カテゴリ**: Backend tmux内部

**問題**: `window.Panes[paneIdx]` が nil の可能性がある。他の箇所（`copyPaneSlice`, `cloneSessionForRead`）では nil pane 防御があるが、ここにはない。

```go
return window.Panes[paneIdx], false, nil  // panes[paneIdx] が nil → 呼び出し元で NPE
```

同様の問題: `activePaneInWindow` (`session_manager_targets.go` L440-447)
```go
return window.Panes[window.ActivePN], nil  // could be nil
```

**修正方法**:
```go
pane := window.Panes[paneIdx]
if pane == nil {
    return nil, false, fmt.Errorf("pane at index %d is nil", paneIdx)
}
return pane, false, nil
```

---

### I-3: `handleKillPane` でのライブポインタ経由の `target.Window` / `target.Window.Session` 読み取り 🟠

**ファイル**: `myT-x/internal/tmux/command_router_handlers_pane.go` (L355-362)
**カテゴリ**: Backend tmux内部

**問題**: `ResolveTarget` からのライブポインタ `target` に対して、ロック不在の状態でポインタチェーン（`target.Window.Session.Name`, `target.Window.ID`）をたどっている。

```go
terminal := target.Terminal  // interface read - 潜在的 race
if target.Window != nil && target.Window.Session != nil {
    sessionName = target.Window.Session.Name
    preferredWindowID = target.Window.ID
}
```

**修正方法**: `handleSendKeys` と同様のスナップショットパターンを使用するか、`GetPaneContextSnapshot` を利用。

---

### I-4: `ResolveDirectionalPane` のクランプ動作が tmux の wrap-around と異なる 🟡

**ファイル**: `myT-x/internal/tmux/session_manager_targets.go` (L213-219)
**カテゴリ**: Backend tmux内部

**問題**: 先頭ペインで `-L`/`-U` を指定するとインデックスが 0 にクランプされ、同じペインが返る。tmux ではラップアラウンドする（最後のペインに移動する）。

```go
if idx < 0 {
    idx = 0  // tmux は idx = len(panes)-1 にラップ
}
if idx >= len(panes) {
    idx = len(panes) - 1  // tmux は idx = 0 にラップ
}
```

**修正方法**: COMPATIBILITY DEVIATION コメントを追加するか、ラップアラウンド動作を実装。

---

### I-5: `ListPanesByWindowTarget` の `allInSession` パスでの `copyPaneSlice` 未使用 🟡

**ファイル**: `myT-x/internal/tmux/session_manager_pane_io.go` (L37-50)
**カテゴリ**: Backend tmux内部

**問題**: `allInSession` パスのインライン pane コピーロジックは、新規追加された `copyPaneSlice` 関数と完全に同じ処理。DRY 原則違反。

**修正方法**:
```go
out := make([]TmuxPane, 0)
for _, window := range session.Windows {
    out = append(out, copyPaneSlice(window.Panes)...)
}
```

---

### I-6: `emitLayoutChangedForSession` のログレベル変更が過剰 🟡

**ファイル**: `myT-x/internal/tmux/command_router_handlers_pane.go` (L302-316)
**カテゴリ**: Backend tmux内部

**問題**: `slog.Debug` → `slog.Warn` に変更された3箇所。これらは正常な並行処理フロー（killPane と layout イベントの race）で発生しうるため、`Warn` レベルは過剰。

**修正方法**: `slog.Info` が適切。

---

### I-7: `resolveSessionTargetLocked` の命名が Locked/RLocked 規約と不一致 🟡

**ファイル**: `myT-x/internal/tmux/session_manager_targets.go` (L281-285)
**カテゴリ**: Backend tmux内部

**問題**: `*Locked` = 書き込みロック必須、`*RLocked` = 読み取りロック必須の規約に対し、`resolveSessionTargetLocked` は読み取り専用関数。コメントで「Despite the 'Locked' suffix, this is safe under either RLock or Lock」と補足しているが規約と矛盾。

**修正方法**: 規約コメントを「`*Locked` は RLock/Lock どちらでも可」に更新するか、関数名を変更。

---

### I-8: `useTerminalSetup` — `disposed` チェック後の `term.write(replay)` 競合窓 🟡

**ファイル**: `myT-x/frontend/src/hooks/useTerminalSetup.ts` (L133-140)
**カテゴリ**: Frontend

**問題**: `GetPaneReplay` の `.then` 内で `disposed` チェック後に `term.write(replay)` を呼ぶが、`term.dispose()` が先に呼ばれた場合に例外がスローされる可能性。

**修正方法**: try-catch で囲む:
```typescript
.then((replay) => {
    if (disposed || !replay) return;
    try { term.write(replay); } catch { /* term already disposed */ }
})
```

---

### I-9: `useWindowRename` — Guard 1 と Guard 2 の共存理由が不明確 🟡

**ファイル**: `myT-x/frontend/src/hooks/useWindowRename.ts` (L100-134)
**カテゴリ**: Frontend

**問題**: `commitWindowRename` は `renamingWindowId` のクロージャと `inflightWindowIdRef` の二重ガードを持つが、両方が必要な理由がコメントで十分に説明されていない。

**修正方法**: Guard 1 のコメントに「React batching により renamingWindowId がまだ更新されていないケース」と明記。

---

### I-10: `ConfirmDialog` — フォーカストラップで Tab キーの `stopPropagation` 未呼び出し 🟡

**ファイル**: `myT-x/frontend/src/components/ConfirmDialog.tsx` (L23-48)
**カテゴリ**: Frontend

**問題**: Escape キーで `e.stopPropagation()` を呼んでいるが、Tab キーでは呼んでいない。ダイアログ表示中に `usePrefixKeyMode` 等の他のキーハンドラと競合する可能性（低リスク）。

**修正方法**: Tab ハンドラにも `e.stopPropagation()` を追加。

---

### I-11: `removeLegacyDirectory` の親ディレクトリ走査で初期値の正規化が不完全 🟡

**ファイル**: `myT-x/internal/install/shim_cleanup_windows.go` (L162-175)
**カテゴリ**: Config/Install/IPC

**問題**: `current := legacyBase` が `filepath.Clean` されていないため、`normalizedLocalAppData` との比較で末尾スラッシュの有無による不一致が理論上起こりうる。

**修正方法**: `current = filepath.Clean(legacyBase)` で初期値を正規化。

---

### I-12: `applyModelOverride` でインライン `--model=value` が未検出 🟡

**ファイル**: `myT-x/cmd/tmux-shim/model_transform.go` (L199-217)
**カテゴリ**: tmux-shim

**問題**: `anyModelFlagPattern` は `--model<space>` のみに対応し `--model=` は検出しない。`--model=value` がより大きな文字列内に埋め込まれた場合、ALL ワイルドカードでも置換されない。

**影響**: 低。Claude Code は現状 `--model value`（空白区切り）形式を使用するため実害は少ない。

**修正方法**: 正規表現にスペース `--model=` パターンを追加:
```go
anyModelFlagPattern = regexp.MustCompile(`(?i)(--model(?:\s+|=))\S+(\s|$)`)
```

---

### I-13: テストの重複が多い (tmux-shim) 🟡

**ファイル**: `myT-x/cmd/tmux-shim/main_test.go`
**カテゴリ**: tmux-shim

**問題**: `TestParseCommandNewCommands` と個別テスト関数で同一シナリオが重複（4箇所以上）。テスト保守コストが不必要に増大。

**修正方法**: 統合するか、`TestParseCommandNewCommands` 側の該当ケースを削除。

---

### I-14: `requireSessionsWithPaneID` のポインタ引数パターンが非Go慣用的 🟡

**ファイル**: `myT-x/app_guards.go` (L27-34)
**カテゴリ**: Backend App層

**問題**: `requireSessionsWithPaneID(paneID *string)` は呼び出し元の変数を変更するためにポインタ引数を使用。Go 慣用パターンでは値返却が推奨。DRY化の効果は大きく機能的には問題なし。

**修正方法（任意）**: `func (a *App) requireSessionsWithPaneID(paneID string) (string, *tmux.SessionManager, error)` に変更。

---

### I-15: `TestEmitSnapshot` — テスト名文字列への一致による条件分岐が脆弱 🟡

**ファイル**: `myT-x/app_events_test.go`
**カテゴリ**: テスト品質

**問題**: `if tt.name == "emits full snapshot on first call"` でペイロード検証を分岐。テスト名変更時に検証がスキップされるリスク。

**修正方法**: テーブルに `wantPayload bool` フィールドを追加。

---

## テストカバレッジギャップ（3件）

### T-1: `ResolveDirectionalPane` — 新規公開関数にテスト無し 🔴

**ファイル**: `myT-x/internal/tmux/session_manager_targets.go`

新規追加された公開メソッド。`DirNone`/`DirPrev`/`DirNext` の各方向、境界値、エラーケースのテーブル駆動テストが必要。

---

### T-2: `ApplyLayoutPresetToActiveWindow` — 直接テスト無し 🟠

**ファイル**: `myT-x/internal/tmux/session_manager_pane_io.go`

`App.ApplyLayoutPreset` 経由で間接テストされているが、SessionManager レベルの直接テストが存在しない。

---

### T-3: `buildPaneEnv` / `isBlockedEnvironmentKey` — テスト無し 🟠

**ファイル**: `myT-x/internal/tmux/command_router.go`

環境変数注入とブロックリストフィルタリングの重要なロジック。テーブル駆動テストが必要。

---

## Suggestions（改善提案: 8件）

| # | ファイル | 内容 |
|---|---------|------|
| S-1 | `app_events.go` L93 | `handleLegacyMapPaneOutput` の未サポート型ログを `slog.Debug` → `slog.Warn` にする検討 |
| S-2 | `app_snapshot_delta.go` L49-125 | `IMPORTANT` コメント5箇所が繰り返し。ファイルコメントで一度言及し各関数は簡略化 |
| S-3 | `app_snapshot_metrics.go` L78-81 | 固定バイト数 `103` のガードテスト追加を検討 |
| S-4 | `useTerminalSetup.ts` L86-90 | `setRendererMode` → `logRendererMode` にリネーム（ミスリーディング） |
| S-5 | `types/tmux.ts` L1-14 | DRIFT RISK コメントは有用。CI で型一致を検証する仕組みを将来検討 |
| S-6 | `session_manager_test.go` | `cloneSessionSnapshots` テストにフィールドカウントガード追加 |
| S-7 | `key_table_test.go` | `TranslateSendKeys(nil)` / `TranslateSendKeys([]string{})` のテスト追加 |
| S-8 | `model_transform.go` L245-248 | `applyFromToReplacement` の defense-in-depth に `debugLog` 追加 |

---

## Positive Observations（良い点）

### Backend App層
- `snapshotEventPolicies` テーブル駆動化による優れた DRY 改善（`app_events.go`）
- TOCTOU 修正 — `ApplyLayoutPreset` の原子化（`app_pane_api.go`）
- `handlePaneOutputEvent` / `handleLegacyMapPaneOutput` の関数分離
- `requireSessionsWithPaneID` による 7メソッドの DRY化
- ロック順序ドキュメントの更新（`app.go`）
- `resolvePaneTitle` のフォールバック優先順位6段階の文書化

### Backend tmux内部
- TOCTOU 修正が体系的（3箇所で単一ロックスコープに統合）
- RLock/Lock アップグレードパターン（`ResolveTarget`）の正確な実装
- 値コピー返却（`[]TmuxPane`）によるデータ競合排除
- `bestEffortSendKeys` / `buildPaneEnv` の共通化
- `INVARIANT`, `REQUIRES` 等の注釈による一貫したロック契約の文書化

### tmux-shim
- エラー時は原文転送する仕様への完全準拠
- ALL ワイルドカードの堅実な設計・テスト
- `runTransformSafe` による panic recovery + snapshot rollback
- `validateTargetFlag` の DRY 抽出

### Frontend
- フック分離の粒度が適切（SRP 遵守）
- EventsOn の戻り値による個別解除（他リスナーの誤解除防止）
- `useBackendSync` の `Promise.allSettled` による並行フェッチ改善
- `usePrefixKeyMode` の `useTmuxStore.getState()` 直接参照
- `useWindowRename` の inflight guard 設計
- WebGL addon の try-catch 強化

### テスト品質
- テーブル駆動テストの一貫した使用
- ディープコピー検証の徹底
- nilガードテストの充実
- ConPTYリーク防止テスト（`TestRemoveWindowByID_PreservesTerminalInReturnedPanes`）

### Config/Install/IPC
- `CleanupLegacyShimInstalls` は全エラーログのみ（起動ブロックしない設計）
- ビルドタグの正確な使い分け
- べき等性の設計と検証
- ホワイトリスト方式の安全なファイル削除

---

## タスク修正計画 — 並列作業可否表

以下の表は、各指摘事項を修正する際に**他の修正と並列で作業可能かどうか**を示しています。

### 凡例
- ⭕ **並列OK**: 他のタスクと独立して同時に作業可能
- ⚠️ **条件付き並列**: 同一ファイルの他修正との競合あり。記載の条件下で並列可能
- 🔗 **依存**: 先行タスクの完了を待つ必要あり

| 優先度 | ID | 概要 | 対象ファイル | 並列可否 | 補足 |
|--------|-----|------|-------------|---------|------|
| 🔴 Critical | C-1 | handleKillPane 二重Close | `command_router_handlers_pane.go` | ⚠️ I-3, I-6と同ファイル | C-1を最初に修正推奨。I-3, I-6は C-1修正後に別途対応 |
| 🟠 Important | I-1 | IME composition リサイズ漏れ | `useTerminalResize.ts` + `useTerminalEvents.ts` | ⭕ | フロントエンド。バックエンド修正と完全に独立 |
| 🟠 Important | I-2 | resolveWindowPaneTarget nil | `session_manager_targets.go` | ⚠️ I-4, I-7と同ファイル | 変更箇所が離れているため実質並列可能だが、マージ時に注意 |
| 🟠 Important | I-3 | handleKillPane ライブポインタ | `command_router_handlers_pane.go` | 🔗 C-1待ち | C-1と同一関数のため、C-1修正後に対応 |
| 🟡 Important | I-4 | DirectionalPane クランプ vs ラップ | `session_manager_targets.go` | ⚠️ I-2, I-7と同ファイル | コメント追加のみなら低リスクで並列OK |
| 🟡 Important | I-5 | copyPaneSlice 未使用 (DRY) | `session_manager_pane_io.go` | ⭕ | 対象ファイルが他修正と重複しない |
| 🟡 Important | I-6 | ログレベル Warn→Info | `command_router_handlers_pane.go` | 🔗 C-1待ち | C-1と同ファイルのため順次対応推奨 |
| 🟡 Important | I-7 | Locked命名規約 | `session_manager_targets.go` | ⚠️ I-2, I-4と同ファイル | コメント修正のみなら低リスク並列可 |
| 🟡 Important | I-8 | term.write replay try-catch | `useTerminalSetup.ts` | ⭕ | フロントエンド。他修正と完全に独立 |
| 🟡 Important | I-9 | useWindowRename コメント明確化 | `useWindowRename.ts` | ⭕ | フロントエンド。他修正と完全に独立 |
| 🟡 Important | I-10 | ConfirmDialog Tab stopPropagation | `ConfirmDialog.tsx` | ⭕ | フロントエンド。他修正と完全に独立 |
| 🟡 Important | I-11 | filepath.Clean 初期値正規化 | `shim_cleanup_windows.go` | ⭕ | installパッケージ。他修正と完全に独立 |
| 🟡 Important | I-12 | --model= インライン正規表現 | `model_transform.go` | ⭕ | shimパッケージ。他修正と完全に独立 |
| 🟡 Important | I-13 | テスト重複削除 | `main_test.go` (shim) | ⭕ | shimテスト。他修正と完全に独立 |
| 🟡 Important | I-14 | ポインタ引数パターン | `app_guards.go` + 呼び出し元 | ⭕ | app層。tmux内部修正と独立 |
| 🟡 Important | I-15 | テスト名条件分岐 | `app_events_test.go` | ⭕ | テストのみ。他修正と完全に独立 |
| 🟠 Test Gap | T-1 | ResolveDirectionalPane テスト | `session_manager_targets_test.go` (新規) | ⭕ | テスト追加のみ |
| 🟠 Test Gap | T-2 | ApplyLayoutPresetToActiveWindow テスト | `session_manager_pane_io_test.go` | ⭕ | テスト追加のみ |
| 🟠 Test Gap | T-3 | buildPaneEnv テスト | `command_router_test.go` | ⭕ | テスト追加のみ |

---

### 推奨作業順序

```
Phase 1 (最優先 — 単独作業)
  └─ C-1: handleKillPane 二重Close修正

Phase 2 (並列作業可能 — 最大5並列)
  ├─ [Backend]  I-2 + I-4 + I-7 (同一ファイル: session_manager_targets.go)
  ├─ [Backend]  I-3 + I-6 (同一ファイル: command_router_handlers_pane.go, C-1完了後)
  ├─ [Backend]  I-5 (session_manager_pane_io.go)
  ├─ [Frontend] I-1 + I-8 + I-9 + I-10 (各フック/コンポーネント独立)
  └─ [Other]    I-11 + I-12 + I-13 (各パッケージ独立)

Phase 3 (テスト追加 — 並列作業可能)
  ├─ T-1: ResolveDirectionalPane テスト
  ├─ T-2: ApplyLayoutPresetToActiveWindow テスト
  └─ T-3: buildPaneEnv テスト

Phase 4 (低優先)
  ├─ I-14: ポインタ引数パターン (広範囲影響のためリファクタリング判断が必要)
  └─ I-15 + S-1〜S-8: Suggestions対応
```

---

### 統計サマリ

| 分類 | 件数 |
|------|------|
| 🔴 Critical Issues | 1 |
| 🟠🟡 Important Issues | 15 |
| 🟠 Test Coverage Gaps | 3 |
| 💡 Suggestions | 8 |
| ✅ Positive Observations | 25+ |

**推奨アクション**: C-1を最優先で修正後、Phase 2, 3を並列で進行。
