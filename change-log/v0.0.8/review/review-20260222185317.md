# コードレビュー結果報告（PR用）

## 1. 概要
本レビューは提供された差分（Diffファイル計44ファイル）と本番コードの一部コンテキストに対する包括的な品質チェックの結果です。`review-pr-para.prompt.md` の方針に基づき、各カテゴリ（バックエンド・フロントエンド・内部状態管理等）ごとに深い視点で、潜在的バグ・保守性の改善点・周辺影響・一括修正可能な類似点という観点から分析しています。

全体的に、以下の点で非常に優れた堅牢なコードとして設計されています。
- **ロックの実装・TOCTOU対策**: ファイルをLstatしてからオープンする際のファイルサイズ制限など、OS・ファイルシステムレベルの競合について防御的なプログラミングが行われている点。
- **IPCの安定化 (Ping-Fetchパターンの導入)**: Wailsイベント（IPC）が過負荷にならないよう、大容量ペイロードを伴うイベント（`app:error-logged`）を `app:session-log-updated` というペイロード無しのPing通知に切り替え、フロント側からAPI経由で取得（しかもDebounce制御・シーケンス番号管理付き）するというアーキテクチャへの進化は、安定性観点で素晴らしい設計です。
- **DXの向上**: `import.meta.env.DEV`時だけ警告をコンソールに出すロジックの追加や、エラーハンドラにおけるロールバック内部エラーの隠蔽など、エンドユーザ・開発者双方にとって親切な実装が行われています。

---

## 2. 指摘事項・レビュー詳細

### 🔴 Critical Issues（マージ前に必須の修正）
現状、アプリケーションのクラッシュや致命的な動作不良に直結するようなIssueは**発見されませんでした**。非常に安定した変更です。

### 🟠 Important Issues（影響範囲等から修正・確認を推奨）
- **`app_session_log.go` - ログファイルのクリーンアップ（ソート）機能の継続性担保**
  - **対象箇所**: `cleanupOldSessionLogs()` 内の `sort.Strings(logFiles)`
  - **内容**: ファイル名フォーマットが `session-YYYYMMDD-HHMMSS-PID.jsonl` であり、文字長とゼロ埋めが担保されている前提でOS非依存の文字列ソートが成り立っています。
  - **確認・対応策**: 将来的にフォーマット（例: PIDの桁数やミリ秒の追加など）を変更した際にソートが崩れる可能性があります。変更前に「ファイル名の文字列ソートで日時順が担保されること」を注意喚起するインラインコメントを `session-logs` の定数定義付近か `initSessionLog` に残しておくことを推奨します。
  
- **UI側での `Truncated` フラグに対する対応確認**
  - **対象箇所**: `app_devpanel_api.go` (`DevPanelWorkingDiff`) の `devPanelMaxDiffSize` / `devPanelMaxUntrackedFilePaths` 溢れ時の `Truncated=true`。
  - **内容**: APIとして限界を超過した場合に `Truncated` が true となる処理が入りました。フロントエンドでこれを受け取った際、ユーザーに「差分が大きすぎるため省略表示されています」といったフィードバックを行っているかについて、（DiffViewer.tsx 側に `.diff-header-truncated` CSSはあるため対応されている可能性が高いですが）確実に描画されるか動作確認を行ってください。

### 🟡 Suggestions（類似処理の一括修正や保守性向上）
- **一括修正: ログプレフィックス `[DEBUG-XXX]` の一斉排除の確認**
  - **内容**: 今回のPRで `[DEBUG-PANE] -> [PANE]`、`[DEBUG-WS] -> [WS]`、`[DEBUG-SYNC] -> [SYNC]` など、GoとTypeScript双方でDebugプレフィックスを取り除くリファクタリングが行われています。
  - **対応策**: 類似処理の修正漏れを防ぐため、プロジェクト全体に対して `grep -r "\[DEBUG-" .` などのコマンドを実行し、変更漏れとなっている箇所（他APIや未修正コンポーネント）がないか並列タスクとして一括で修正・統一することを推奨します。

- **JS: フックでの再取得シーケンスの制御 (`useBackendSync.ts`)**
  - **内容**: 新規追加された `errorLogFetchSeq` による競合制御は堅牢ですが、`import.meta.env.DEV` 配下でリトライ等も出力されています。このリトライ処理内で万が一コンポーネントがアンマウント状態になった際、即座に return される設計（`!isMountedRef.current`）は機能しています。今後の同様のフックでもこの「シーケンス番号＋キャンセル判定」パターンが利用できるよう、Utility Hook (例 `useLatestPromise`) として切り出すと将来の保守性が高まります。

- **`useErrorLogStore.ts` における既読ステータスのリセット動作**
  - **内容**: `maxNewSeq > 0 && maxNewSeq < lastReadSeq` という条件でバックエンド再起動時のシーケンス初期化を検知しています。これは妥当ですが、エッジケースとして「バックエンド再起動と同時に新しく大量のログが溜まり、過去の `lastReadSeq` を偶然超えてしまった場合」には未読状態が復元されない可能性があります。再起動の明示的なイベント（`config:updated`の中身など）を検知してStoreをリセットにすることで、100%確実な初期化も可能です。

- **`ErrorLogView.tsx` の即時既読化**
  - **内容**: `useLayoutEffect` によって、エラーログのビューを開いた瞬間にすべてのログが既読（`unreadCount = 0`）になります。運用上、ビューを開いただけで画面外にある未読エラーログまですべて既読となってしまうため、将来的に Intersection Observer を用いて「視界に入ったタイミングで既読」にするような改修タスクの積むことをお勧めします。

---

## 3. Recommended Action (作業方針)
1. **Critical/Importantの確認**: UI側の Truncated 表示の実機動作確認と、ファイル名フォーマットに対する将来のソート崩れ警告のインラインコメント追加。
2. **全体Grep**: `[DEBUG-*]` の除去漏れの修正を実施。
3. **並列タスク化**: 修正・確認作業の並列化を進める（下記の「タスク修正・並列作業可否表」を参照）。

---

## 4. サブエージェント向け・タスク修正に関する並列作業可否表

差分量が多いため、サブエージェント等で効率的に修正・改修を分担できるようカテゴリと依存関係を整理しました。
各カテゴリは「並列作業可能」として設計できます。作業者目線で「どこに気をつけて作業・確認すべきか」を記載しています。

| タスク（カテゴリ） | 並列作業の可否 | 依存するタスク・ボトルネック | 作業内容・確認観点（サブエージェント向け指示） |
| :--- | :---: | :--- | :--- |
| **A. API・Backend（DevPanel関連）** | 🟢 可能 | `types.go` の定義（完了済み） | `app_devpanel_*.go` に対する保守コメントの追加。`isFreshRepoStatusOutput` 等のテスト網羅率の確認。他モジュールへの影響がないため独立して作業可能。 |
| **B. API・Backend（SessionLog関連）** | 🟢 可能 | なし | `app_session_log.go` のシーケンス番号制御とPIDを含むファイル名制約に関するコメント（`sort.Strings`時の注意書き）の追加作業。単独で安全に作業可能。 |
| **C. 一括置換リファクタリング** | 🟢 可能 | なし | リポジトリ全体に対して `[DEBUG-` というテキストの検索・置換を実施するタスク。他タスクのコードに影響しないよう正規表現・単純置換での対応であり、別プロセスで実行可能。 |
| **D. Frontend（Store & Hooks）** | 🟡 条件付可能 | Cタスク(プレフィックス置換)との競合 | `useBackendSync.ts`, `errorLogStore.ts` におけるPing-Fetch関連の挙動確認と、バックエンド起動検知のリセット処理の調整。バックエンド側のAPI IFは確定しているので、フロント単独でテスト可能。プレフィックス名修正と同時の際はマージコンフリクトに注意。 |
| **E. Frontend（UI View & CSS）** | 🟢 可能 | なし | `ErrorLogView.tsx`, `ViewerSystem.tsx`, `DiffViewer.tsx` などの見た目・操作性の最終確認と一部スタイリング（CSS変数など）の調整。Truncated表示の確認や、既読ロジックに対するIntersection Observer導入の検討（Issue化）のみのため並列実行可能。 |

### ★並列作業の推奨フロー
- **Agent 1** が **A** と **B** (バックエンドの保守コメント追加や実テストロジック確認) を担当。
- **Agent 2** が **D** と **E** (フロントエンドのStoreロジック確認、ログ置換漏れ部分のUIレベル確認等) を担当。
- **Agent 3 (または一括置換用シェル)** が **C** の `[DEBUG-` 文字列の一括置換を実施。（※コンフリクト防止のため、他の作業者が改修するファイルとは干渉しないように Pull Request を分けるか、最後に行うと安全です。）
